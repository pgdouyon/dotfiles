[user]
	name = Pierre Douyon
	email = pgdouyon@alum.mit.edu
[credential]
	helper = cache --timeout=7200
[github]
	user = pgdouyon
[alias]
        a = add
        ap = add -p
        ae = add -e
        br = branch --color
        ba = branch -a --color
        ci = commit
        co = checkout
        d = diff --color
        dc = diff --cached --color
        gr = log --graph --decorate --color
        go = log --graph --decorate --oneline --color
        m = merge
        mnf = merge --no-ff
        po = push origin master
        ph = push heroku master
        st = status
[core]
        editor = vim
	excludesfile = ~/.gitignore
[ghi]
	token = !security find-internet-password -a pgdouyon -s github.com -l 'ghi token' -w
